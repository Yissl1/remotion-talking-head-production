# 口播音效库规范

## 资源位置

音效资源统一放在：

`C:\Users\11066\.codex\skills\remotion-talking-head-production\assets\sfx\library`

索引与时间轴位于：

`C:\Users\11066\.codex\skills\remotion-talking-head-production\assets\sfx\sfxLibrary.ts`

## 音效分类

| ID | 文件 | 适用节点 | 默认音量 |
|---|---|---|---:|
| `spaceKey` | `space-key.mp3` | 操作演示中的空格键或播放控制动作 | 0.07 |
| `woosh` | `woosh.mp3` | 大段转场、章节切换和对比段切换 | 0.11 |
| `swish` | `swish.mp3` | 组件、章节标题和结论卡的短促进入或切换 | 0.12 |
| `cashRegister` | `cash-register-ding.mp3` | 收益、金额、免费额度和奖励数字出现 | 0.10 |
| `xiu` | `xiu.mp3` | 快速飞入、箭头划过和轻快元素切换 | 0.09 |
| `success` | `success.mp3` | 正确、完成、通过和正向结果确认 | 0.08 |
| `error` | `error.mp3` | 放弃、成本压力、错误和负向结果 | 0.08 |
| `mouseClick` | `mouse-click.mp3` | 鼠标点击、按钮触发和重点节点确认 | 0.07 |
| `keyboardTyping` | `keyboard-typing.mp3` | 输入提示词、工作流或 Skill 文本 | 0.06 |
| `notification` | `notification-ding.mp3` | 通知到达、消息提醒和普通提示节点 | 0.08 |
| `sua` | `sua.mp3` | 素材页、录屏或证据画面的快速切入 | 0.10 |
| `boodong` | `boodong.mp3` | 轻提示、状态确认、平台扶持或反馈标签 | 0.09 |

## 使用门槛

1. 音效必须绑定到可解释的画面动作或口播语义，不按字幕逐字机械铺设。
2. 人声优先；默认音量约为 `0.05–0.14`，不得盖住口播。原素材已有明显原声时，只使用低音量点缀。
3. 同一信息段最多一个主转场音效，另外只添加少量语义事件音效。
4. 操作类音效仅在画面确实展示对应鼠标或键盘动作时使用。
5. 音效起点必须与组件入场、素材切入、章节切换或语义节点对齐。
6. `swish`、`woosh`、`xiu`、`sua` 按动作大小和场景区分，不机械互换。
7. `success`、`notification`、`boodong` 分别对应正向完成、普通通知和柔和状态确认。

## 当前来源

当前 12 个 MP3 均来自用户在 2026-08-27 提供的新音效文件，直接复制、未重新编码。此前合成重建的 MP3 与 WAV 母版已删除。

## 渲染验收

最终导出必须确认音效轨存在、音量低于人声，并抽查章节切换、数字出现、证据素材切入、通知、正向结果和负向结果节点。
