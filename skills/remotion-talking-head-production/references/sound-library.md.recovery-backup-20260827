# 口播音效库规范

## 资源位置

本项目音效资源统一放在：

`C:\Users\11066\Documents\ChatGPT\新剪辑测试\public\sfx\library`

索引与时间轴位于：

`C:\Users\11066\Documents\ChatGPT\新剪辑测试\src\sfxLibrary.ts`

## 音效分类

| ID | 文件 | 适用节点 |
|---|---|---|
| `swish` | `swish.mp3` | 信息组件、章节标题、结论卡的短促入场/切换 |
| `cashRegister` | `cash-register-ding.mp3` | 收益、金额、免费额度、互动收益等数字结果 |
| `boodong` | `boodong.mp3` | 轻提示、状态确认、平台扶持、反馈标签 |
| `sua` | `sua.mp3` | 素材页、录屏、证据画面切入 |
| `woosh` | `woosh.mp3` | 大段转场、章节切换、对比段切换 |
| `error` | `error.mp3` | 放弃、成本压力、错误或负向结果 |
| `spaceKey` | `space-key.mp3` | 明确出现空格键/播放控制动作的操作演示 |
| `mouseClick` | `mouse-click.mp3` | 鼠标点击、按钮触发、节点确认 |
| `keyboardTyping` | `keyboard-typing.mp3` | 明确出现输入提示词、工作流文本的操作演示 |

## 使用门槛

1. 音效必须绑定到可解释的画面动作或口播语义，不按字幕逐字机械铺设。
2. 人声优先；默认音量约为 `0.05–0.14`，不得盖住口播。原素材已有明显原声时，只使用低音量点缀。
3. 同一信息段最多一个主转场音效，另外只添加少量语义事件音效。
4. 操作类音效仅在画面确实展示对应鼠标/键盘动作时使用；普通口播不使用键盘声。
5. 音效的起点必须与组件入场、素材切入、章节切换或语义节点对齐；音效时长按素材实际长度裁切。
6. 禁止使用音效制造闪屏、卡帧、抖动或改变既有画面节奏。

## 本期执行

本期时间轴由 `AUG25_SFX_CUES` 维护。已按 SRT 语义布置：章节进入使用 `swish/woosh`，数字收益使用 `cashRegister`，负向结果使用 `error`，证据素材切入使用 `sua`，平台/状态提示使用 `boodong`，仅在明确操作节点使用 `mouseClick`。`keyboardTyping` 与 `spaceKey` 保留在库中，但本期没有在非操作段强行使用。

## 渲染验收

最终导出必须保留原画面视频流，确认音效轨存在、时长与原片一致、音效低于人声，并抽查章节切换、数字出现、证据素材切入和负向结果节点。
