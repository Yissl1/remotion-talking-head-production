---
name: remotion-talking-head-production
description: 创建或重做 16:9 Remotion 口播视频：确认风格后分析 SRT 与真实素材，使用沉淀好的组件库、克制转场和共享音效库完成包装；真实录屏优先，人物与字幕保持全局同步。禁止调用独立动效库、Shotcraft 效果和撕纸风格。
metadata:
  short-description: 以组件库、转场和音效完成科技口播剪辑，禁用独立动效库
---

# Remotion 口播包装制作

仅使用本 Skill 的风格体系与用户本次提供的参考。不要读取、沿用或混合旧的撕纸风格 Skill。

## 核心目标

把人物观点、真实界面证据、用户确认的插画与结构化信息表达组合成统一的科技资讯可视化视频。人物建立信任，真实素材和插画证明/解释观点，MG 与信息组件降低理解成本。插画只是新增的一类媒体素材，必须服从本 Skill 既有的口播包装、布局、安全矩形、时间码和渲染逻辑；插画 Skill 本身的画风与提示词体系不得被本 Skill 改写。

## 新项目启动规则

安装并调用本 Skill 创建新的口播视频项目时，第一条用户可见回复必须直接询问：

> 这次有没有喜欢的剪辑风格、参考图片、参考视频或链接？如果有，请发给我，并说明希望参考哪些部分；如果没有，我将按照已经沉淀好的视频制作规则去制作。

这一询问必须发生在读取、分析视频、SRT、参考资料或素材目录之前。即使用户创建项目时已经附上全部文件，也先完成风格确认。询问过一次后不得重复询问，也不要要求用户再次发送“开始”。

## 风格选择后的关键行为

当用户回答“没有参考”“按沉淀规则制作”“按已经沉淀好的视频制作规则去做”“按最终版风格制作”或同义表达时，启用固定预设 `deposited-final-v1`，但**不得立刻生成剪辑脚本或渲染视频**。必须先进入第一步“分析素材、生成并确认插画（可选 MG）”：

> 好的，本项目将按照已经沉淀好的视频制作规则进行。现在进入第一步：请提供主口播视频、SRT、已有产品录屏/截图/图片/Logo 等素材。我会先分析 SRT 和已有素材，判断哪些语义适合生成插画，调用独立插画 Skill 逐张生成并展示给你选择。插画确认后，才会把它们和你提供的素材一样登记为普通媒体素材并进入剪辑脚本。只有你明确提出 MG 需求时，我才另外进入 MG 制作；如果本期不需要 MG，可以直接说明。

用户在创建项目时已经附上视频、SRT 或产品素材时，不重复索要这些文件；应直接分析已提供内容，但仍停留在第一步，先完成插画生成与用户选择确认。未确认插画（或用户明确表示不需要插画）前，禁止生成脚本或渲染视频。

完整状态和门控见 [工作流状态与门控](references/workflow-state-gates.md)。核心状态为：

```text
awaiting-style
awaiting-materials
analyzing-srt-and-assets
generating-illustrations
awaiting-illustration-selection
mg-production
awaiting-script-request
awaiting-script-confirmation
rendering-30s-preview
awaiting-preview-confirmation
rendering-final
completed
```

## 已沉淀规则执行模式

当用户选择沉淀规则时，读取并执行：

- `references/final-style-preset.md`：最终版的可执行视觉、布局、节奏与验收预设。
- `references/style-core.md`：风格不变量和禁用项。
- `references/component-library.md`：场景与组件选择。
- `references/step-1-material-components.md`：SRT、真实素材、插画确认和可选 MG。
- `references/illustration-integration.md`：把已确认插画作为普通媒体素材接入原有剪辑风格。
- `references/step-3-final-render.md`：Remotion 组装、30 秒预览、渲染和成片验证。
- `assets/remotion-components/components.tsx`：必须复制或导入目标工程并真实调用的组件源码。
- `assets/edit-script.schema.json`：脚本结构和预设标识。

这些文件用于实际制作，不是只读答询材料。执行必须遵守四个门控节点：

1. 插画确认门：分析 SRT 后先展示逐张插画，用户选择/确认具体插画；用户也可以明确表示本期不需要插画。单独说“继续”不等于确认。
2. 第一步结束门：插画已确认或明确不需要，且用户明确说“下一步”“素材准备好了”“开始生成脚本”等，才能进入脚本阶段。MG 只有用户明确提出时才进入，不是必经步骤。
3. 脚本确认门：脚本生成后，必须把 `edit-plan.md` 和 `edit-script.json` 交给用户确认；用户确认前禁止渲染 30 秒预览。
4. 预览确认门：用户确认 30 秒预览没有问题后，才能渲染完整成片；用户提出修改时，回到脚本、组件或 30 秒预览阶段，禁止直接出全片。

除非用户明确要求跳过某道门，不能把多个阶段合并在一次回复中，也不能自动替用户确认。

目标是复现最终确认版本的同一视觉体系和制作逻辑。新视频的人物构图、语义、素材数量会不同，因此不要求逐像素复制，但布局模式、字幕规格、组件风格、动画节奏、同步方式、防剧透规则和验收标准必须一致。

## 固定三步工作流

### 第一步：分析素材、生成并确认插画（可选 MG）

风格选择完成后，先收集并分析：主口播视频、精确 SRT、产品录屏/截图/图片/Logo/数据来源。根据 SRT 语义和真实素材判断适合做插画的语义段落，调用独立 `ian-xiaohei-illustrations` Skill 逐张生成插画并展示给用户选择。独立插画 Skill 的提示词、视觉 DNA、IP 规则和 QA 不得被本 Skill 改写。

输出或更新 `style-decision.md`、`material-requirements.md`、`cue-map.json`、`illustration-shot-list.md`、`illustration-assets.json`。根据 SRT 语义告诉用户缺少哪些真实素材，并区分已有素材、需要补拍/补录的素材、已确认插画和可选的 MG 组件。

用户如果明确要求制作 MG，才按描述创建可复用 Remotion 组件/素材，并提供组件静帧或 2–6 秒短动画预览。用户可以多轮补充描述、参考素材并要求继续生成多个 MG；每轮完成后仍停留在第一步。若用户明确表示本期不需要 MG，则跳过 MG 分支，不影响插画确认和后续脚本。

不得在第一步自动生成 `edit-script.json` 的最终镜头编排，不得渲染 30 秒预览或完整成片。详细规则见 [第一步说明](references/step-1-material-components.md)。

### 第二步：生成逐时间码剪辑脚本

只有用户明确结束第一步后，才能基于 SRT、真实素材、第一步已确认的插画、已确认的 MG（如有）和 `deposited-final-v1` 生成 `edit-plan.md` 与 `edit-script.json`。插画只作为一种普通媒体素材参与镜头规划，不改变既有口播包装风格、组件库、布局或渲染逻辑。

脚本必须只引用第一步已确认的插画和 MG 资源路径/组件名；每个镜头写明起止时间、字幕语义、场景模式、布局、素材源、组件、入场/停留/出场、音效、防剧透时间和转场。脚本生成并完成 Schema、防剧透、安全矩形和组件名校验后，必须等待用户明确确认。

详细规则与 JSON 结构见 [第二步说明](references/step-2-edit-script.md)。

### 第三步：生成视频

只有 `scriptConfirmed == true` 才能按确认脚本组装 Remotion 工程并渲染**默认 30 秒代表性预览**。用户明确指定其他预览区间时，才可使用指定区间。

预览输出后，状态切换为 `awaiting-preview-confirmation`。只有 `previewConfirmed == true` 才能沿用同一预设、组件 API 和时间轴脚本渲染全片。默认 1920×1080、30fps、H.264 + AAC。

详细风格、布局、同步、动画、音效和验收规则见 [第三步说明](references/step-3-final-render.md)。

音效库与本期音效时间轴见 [音效库规范](references/sound-library.md)。

## 不可违背的规则

- SRT/真实语音时间码优先于书面脚本；运行画面优先于推测。
- 人物是主体；信息组件只出现在脸部和字幕安全区之外。
- 信息组件不能剧透；卡片内部节点按口播逐项揭示。
- 卡片少量使用，多用无外框艺术字、短标签、数字、流程与证据框。
- 素材展示可有一次性入场和出场，但不得持续抖动、循环缩放、漂移或无意义 Ken Burns。
- 小窗只能播放同一条人物口播，并保持全局时间轴同步；小窗静音，不能重新从 0 播放或使用当前帧作为 `startFrom`。如果人物源视频与主口播不是同一时间起点，必须先用音频波形/嘴型抽样校准 `sourceStartFrame` 偏移，再逐帧验收；禁止默认假设偏移为 0。
- 真实素材优先清晰可读，推荐占画面约 2/3；人物全屏、左右分栏、黑色渐变证据台三种模式交替使用，避免 PPT 感。
- 单张图片信息默认只放人物口播画面右侧，不挡脸；多张图片先叠放，再以人物为中心向两侧展开，分组之间必须更换展开动效。
- 操作演示录屏才允许使用放大追踪和鼠标追踪；普通参考/案例素材直接右侧展示，不做追踪。
- 所有信息卡、艺术字、MG 和素材必须有合理的独立入场、停留与出场；同类转场不连续滥用。
- 背景主题艺术字必须通过 `themePhrase` 配置。当前示例为“死都要死在AI上”，不能成为所有项目的固定默认文案。
- 灰黑偏黑的证据台使用 `#111315` 一类的灰黑底；主题字默认采用横向平行文字带，相邻行左右交错，实心与描边交替。艺术字排版风格固定，但具体文案必须从当前主题的 `themePhrase` 生成。
- 画面中的彩色强调统一改为亮白 `#F7F8FA`；不使用青色、紫色、绿色等彩色组件作为默认视觉重点。
- 可为部分卡片、素材框、数字组件和圆形人物小窗开启边缘发光；发光应克制，只服务于入场和层级，不给所有元素叠加厚重光晕。
- 数据数字必须按口播时间出现：从较小数值快速递增到目标值并停住，不能一开始直接展示最终数字，也不能提前剧透。
- 数字滚动或增长动画优先调用音效库 `numberChange`（`assets/sfx/library/number-change.mp3`），从数值开始变化的同一帧播放一次，且不得盖住人声。
- 音效少而准，始终低于人声，不为每个字幕词配音效。
- 禁止撕纸、长线条穿脸、箭头挡脸、暗部遮脸、无意义 glitch 和装饰性大框堆叠。

## 风格基准

用户选择沉淀规则时，先读取 [最终版可执行预设](references/final-style-preset.md)，再读取 [风格核心](references/style-core.md) 和 [组件库说明](references/component-library.md)。本次有新参考时，仍以用户确认后的 `style-decision.md` 为最高项目依据。

## 完成标准

- 第一步 SRT/真实素材分析已完成，插画已由用户选择确认或用户明确表示不需要插画；MG 仅在用户需要时准备，且用户明确结束第一步。
- 第二步脚本与 SRT 时间码逐项对应，引用已确认插画/素材/MG，并通过防剧透与安全矩形检查，且用户已确认脚本。
- 第三步先交付默认 30 秒预览；只有用户确认预览后才生成完整成片。
- 从最终 MP4 抽查开场、各章节切点、素材入/出点、人物小窗、最长字幕、结论与 CTA；不能只检查 Remotion Studio。
- 交付成片、关键静帧/联系表、脚本、组件源文件及复现命令或项目路径。


## 最新最终视觉基准（v6 平行背景版，优先级高于旧预设）

当用户未提供新的参考，或明确选择“按已经沉淀好的视频制作规则”时，默认复现 `preview-61s-v6-parallel-backdrop.mp4` 所确认的最终包装逻辑：

- 16:9 横屏，灰黑偏黑背景；背景艺术字采用多行横向平行、左右交错、实心与描边交替的排版，文案随主题变化。
- 左上固定使用 `FIELD NOTE / AI COMPANION`、`REPORT STYLE · 16:9 / 30FPS` 和短横线信息，不使用矩形外框、箭头或挡脸长线。
- 人物全屏时人物仍是主体；只在左右安全区放少量无框短标题/语义标签，避开脸、嘴、手和字幕。
- 展示证据时切换到“证据台”：证据素材约占画面 2/3，左上是短标题，左下为同步、静音的人物圆形小窗；普通组件不得与证据素材重叠。
- 录屏、网页或视频证据需要阅读时，采用独占式大画面并去除黑边，默认 `objectFit: cover`，通过 `objectPosition` 裁切；禁止用 `contain` 留出上下黑边。
- 证据素材入场后保持稳定，不做持续抖动、循环缩放或无意义漂移。仅操作演示允许一次有目的的放大/追踪和鼠标标记。
- 所有颜色强调统一为亮白层级；不默认使用 cyan、blue、lime、violet 等色相。层级只用透明度、黑色阴影、白色边缘光晕和描边区分。
- 组件、信息卡、MG 和字幕均需按 SRT 语义同步，分层入场、停留、出场；不得提前剧透结论、数字、产品名或步骤。
- 数字组件从较小值快速递增到目标值，并在目标数字停住；目标数字不得在动画开始前提前显示。

### 证据素材互斥层级

证据台或独占式素材展示期间，关闭会与素材区域重叠的 `ComparePanel`、`TechCard`、`ImageSpread`、普通卡片和装饰线。需要补充信息时只放左上短标题、左下人物圆窗和底部字幕；若素材必须全屏展示，则隐藏普通背景组件，仅保留统一视觉地板与字幕。

## 2026-08-22 纠偏：最终 v6 基准的发光边界与稳定性（最高优先级）

本节是对最终确认版本的纠偏，优先级高于任何旧版示例、旧 Skill 或撕纸风格。它描述的是“最终 v6 平行背景版”已经确认的实际成片规则，不是可选的问答建议。

### 发光必须分层

- 图1、图2的信息卡、短标签、普通组件、字幕和普通素材边框**默认不发光**；不得因为“统一亮白”而给它们加白色外发光。
- 只有证据台/灰黑背景板里的**实心白色背景艺术字**允许使用克制的白色发光；描边艺术字不发光，背景板本身不做白色闪烁。
- 人物圆形小窗、操作演示聚焦圈或少数证据框只能在脚本明确需要时使用很弱的边缘光；这不等同于信息卡发光。
- 普通组件用透明度、细描边、黑色阴影、字号和分层入场建立层级；禁止用厚重光晕代替排版。

### 背景艺术字

- 背景为灰黑偏黑色；艺术字采用多行横向平行、左右交错、实心与描边交替的排版。
- `themePhrase` 是项目级文案输入：本项目使用“死都要死在AI上”；其他项目若用户没有指定，则使用固定英文艺术字，不把本项目中文文案写死为全局默认。
- 艺术字具体内容可以随主题变化，但排版、字距、行距、交错关系和克制发光规则保持一致。

### 严格时间码与防闪屏

- 主口播视频从第 0 帧连续播放；场景切换不得重新挂载、重新 seek、循环或用当前帧重置人物口播轨。
- 所有字幕、素材、信息组件和 MG 只允许在对应 SRT 语义窗口内出现，不得提前剧透数字、产品名、结论或下一句信息。
- 背景切换使用稳定的 opacity 淡入淡出；禁止整块背景缩放、强白色扫屏、单帧白闪和高亮 glitch。
- 真实视频/录屏预加载并用 `cover` 去除黑边；普通素材入场后保持稳定，不做持续抖动、循环缩放或无意义漂移。
- 静态图片在语义窗口内稳定停留不属于编码卡帧；验收时必须区分“内容静止”与“渲染冻结”，并检查场景切点前后连续帧。

### 素材与组件互斥

- 证据素材占据右侧约 2/3 或独占式大画面时，关闭会进入素材区域的普通信息卡、装饰线、箭头和底部组件；只保留左上短标题、左下同步静音人物圆窗和底部同步字幕。
- 普通非操作素材只做稳定展示；只有真实操作演示才允许一次有目的的放大追踪、鼠标追踪和聚焦圈。

### 组件零重叠硬约束（最高优先级）

- **严禁组件重叠。** 任意一帧内，两个信息卡、艺术字、图标、数据条、流程节点、素材框、人物小窗或 MG 的可见安全矩形不得相交；不得挡脸、挡嘴、挡手、挡字幕，也不得让素材和解释组件互相压住。
- 每组组件在脚本中必须登记 `startFrame`、`endFrame`、`rect`（含入场/出场时的最大外扩范围），并生成“时间段 × 安全矩形”检查表；只要相交就必须改时间或改布局，不能靠 z-index 兜底。
- 语义组交接默认采用“先清空、再出现”：上一组所有成员的 `endFrame` 必须小于或等于下一组首成员的 `startFrame`；建议额外留 6–12 帧中性画面，避免旧组件退场残影与新组件入场同帧重叠。
- 只有在已经完成矩形相交检查、且用户明确需要并行表达时，才允许不同语义组同时存在；并行组也必须各自占用独立安全区。
- 每个交接点必须抽查前一帧、清空帧、下一组首帧和入场完成帧；发现残留、闪屏、重叠或卡帧，必须回到脚本修正后再渲染。

## 2026-08-22 最新用户确认纠偏（最高优先级，覆盖本文件更早的冲突示例）

本项目的唯一包装基准是 `C:\Users\11066\Documents\ChatGPT\新剪辑测试\report-style-preview\out\preview-61s-v6-parallel-backdrop.mp4` 及其后续用户确认截图；不得回退到旧撕纸 Skill，也不得把“统一亮白”误读为所有组件纯黑白。

- 信息卡、短标题、字幕使用参考图 5/6/7 的彩色科技编辑体系：深色底、主体白字、红/橙黄/蓝色作为语义强调色；卡片小而克制，不能遮挡脸、嘴、手或字幕。
- 字幕使用同一彩色参考排版：底部居中、短行、深色半透明底板，按 SRT cue 精确入场/出场；组件不得剧透后文。
- 灰黑背景板上的白色艺术字才允许有克制的白色光泽；本期主题文案固定为“死也得死在AI上”。信息卡和字幕不加厚重白光。
- 单张截图默认放人物右侧，使用 contain 保留完整可读内容；横向截图禁止塞入竖向卡片、禁止过度裁切和无意义放大。
- 多张图片必须分层入场，先叠放再向人物两侧铺开；不同组不能重复同一种入场动效；入场完成后静止，不持续抖动、呼吸缩放或漂移。
- 真实录屏/视频若语义要求展示，必须按素材原始时长完整播放；操作演示才可做一次目的明确的放大、鼠标和聚焦追踪。非操作素材稳定展示。
- 证据素材占据画面时与普通信息组件互斥；若需要阅读，采用 cover 去黑边的大画面或独占式展示；播放期间关闭不必要的人物圆窗，避免与素材重叠。
- 人物主轨从第 0 帧连续播放；左下圆窗（若场景需要）必须使用同一主口播轨的全局时间轴、静音、OffthreadVideo，不得重新从 0 播放或循环导致嘴型错位/卡顿。
- 禁止挡脸长线、箭头、强 glitch、单帧白闪和卡帧；组件和 MG 均需有合理分层入场、停留、出场。


## 2026-08-22 全屏证据视频硬规则（最高优先级）

- 对完整操作录屏、旧版成片或用户明确要求“看完整过程”的真实视频，采用证据独占场景：证据视频独占画布，不与人物主画面、普通信息卡、装饰线、箭头、背景艺术字或底部组件重叠。
- 证据视频使用 `cover` 铺满 16:9 画布，去除黑边；必要时使用不透明灰黑底承接切换，但不得让底层人物或组件透出。
- 证据场景开始时直接切入，或使用不透明遮罩完成切换；禁止透明渐入造成主口播、背景字与证据视频叠影，禁止黑屏、白闪、闪屏和卡帧。
- 证据视频从原始素材 0 秒开始连续播放，并按语义要求完整播放到素材结束；不得只截取一秒后跳回口播。素材播放未结束前不得恢复下一段口播包装。
- 证据视频播放期间关闭左下人物圆形小窗；证据结束后再恢复人物口播。若必须保留字幕，仅保留与当前 SRT cue 同步的字幕，不得提前显示后续结论。
- 普通图片、截图和非操作素材不自动套用全屏证据场景；默认放在人物右侧并使用 `contain` 保留完整可读内容，除非脚本或用户明确要求独占展示。
- 证据场景在 Remotion 中必须通过独立 `Sequence`/独立图层实现，并预加载视频；切点验收必须逐帧检查首帧、末帧和恢复口播帧，确认没有底层透出、黑帧、重复 seek 或冻结帧。

## 2026-08-22 插入式证据时间线硬规则（最高优先级）

当口播先说“这是我原来用 AI 生成的视频”“下面看完整录屏”或其他介绍句，随后要求完整播放一段证据视频时，必须采用**插入式时间线**，而不是覆盖式同步播放：

- 先让介绍句按 SRT 完整说完，在该 cue 的结束帧暂停主口播时间线。
- 将证据视频作为独立 `Sequence` 插入，从素材第 0 帧开始，连续播放其完整画面和自身音频；这一段必须独占画布。
- 插入期间停止主口播画面和主口播声音，不显示主口播字幕、人物圆窗、信息卡、MG、背景艺术字或其他包装；任何时刻只能有一个主音频源。
- 证据视频结束后，从暂停的原口播源时间继续播放，禁止跳词、重复、抢话或让口播在幕后继续前进。
- 插入点之后的 SRT、字幕、信息组件、MG、人物圆窗和其他素材节点必须统一顺延证据素材的实际插入时长；不得只移动画面而保留旧时间码。
- Remotion 实现应拆成“插入前主口播 / 独占证据 / 插入后主口播”三段。禁止使用一条全局连续的主口播 `<Audio>` 再仅隐藏人物画面。
- 恢复后的主口播源时间必须等于插入前暂停点；人物圆窗若再次出现，也必须映射到同一原口播源时间，确保嘴型、声音和字幕一致。
- 30 秒预览因插入素材而减少后续原口播覆盖范围是正常结果；不得通过压缩素材、倍速播放或让两条时间线重叠来保留原来的预览终点。
- 验收时逐帧检查：介绍句末帧、证据首帧、证据末帧、口播恢复首帧；并检查音轨，确认插入期间没有主口播残留、双音轨或后续字幕提前出现。

## 2026-08-23 最新确认规则：字幕纯白与组件交接停留（最高优先级）

以下规则覆盖本文件及旧版示例中的冲突描述，适用于用户本次确认的 30 秒预览和之后的项目：

### 字幕
- 字幕只使用纯白文字；包括关键词在内，禁止红、黄、蓝等彩色字体。
- 字幕仍使用参考版的底部居中、短行、语义断行、紧凑深色半透明底板排版。
- 字幕必须严格按 SRT cue 入场与退出；字幕纯白不等于信息卡、标题或图标必须全部改成黑白。

### 灰黑背景板
- 证据展示阶段禁止纯黑背景，使用灰黑偏黑渐变地板（约 `#24272a → #171a1d → #292c30`），保留多行平行交错的主题艺术字。
- 背景艺术字实心白字可有克制光泽；卡片、两侧标题、图标和字幕默认不做厚重白色外发光。
- `themePhrase` 是项目级输入：本期使用“死也得死在AI上”；其他主题可替换内容，不改变排版系统。

### 组件交接停留
- 信息卡、两侧解释文字、图标、数据条、流程节点和 MG 组件不得在当前一句 SRT 结束帧立即全部消失。
- 默认延长到下一段解释组件完成入场后，再以 8–18 帧的退场动画退出；连续解释同一主题时允许跨多个 cue 保持。
- 两组同时存在时先完成新组入场，再退出旧组；同侧组件必须上下/左右错位，禁止互相覆盖、挡脸、挡嘴、挡手或压字幕。
- 交接停留不能让组件提前剧透后续语义；新组件只在对应 SRT 语义开始后出现。
- 每个交接点必须抽帧验收，确认没有瞬间闪退、单帧闪屏、重叠或突然卡住。

### 稳定性
- 素材和静态组件入场完成后保持稳定，不持续抖动、呼吸缩放、漂移或重复 seek。
- 渲染前后检查 30fps 连续帧、场景切点、独占证据音轨和组件交接；用户未确认预览前，不得生成完整成片。

## 2026-08-23 素材播放背景修正（最高优先级）

- 插入式完整证据视频虽然独占画布和音轨，但**不能使用纯黑地板**。素材窗口外必须铺 `DemoBackdrop` 同款灰黑偏黑渐变、网格和横向平行交错主题艺术字。
- 证据素材仍保持独占：人物主画面、人物圆窗、普通信息卡、装饰线、箭头和字幕层全部关闭；只有证据视频窗口和稳定的灰黑艺术字背景存在。
- 证据视频窗口可以居中约占画布 2/3，窗口外露出的背景必须与最终 v6 平行背景版一致；素材本身不透明，避免底层人物透出。
- 证据场景的第一帧允许做短促背景淡入，但不能出现纯黑闪屏或白闪。


## 2026-08-23 本轮操作演示与防重叠补充规则（最高优先级）

- 组件布局在生成脚本阶段先建立安全矩形表，并逐帧检查矩形相交；同一画面中任何信息卡、图标、标题、素材窗口、人物圆窗和字幕都不得重叠。若组件需要交接，先让旧组件退场，再让新组件进入；不能靠半透明叠放“解决”重叠。
- 画面单调时必须增加“组件标注理解”：采用“图标 + 大字标签 + 小字解释”的语义标注组件，说明当前素材、流程或概念；标注只在对应 SRT 语义开始后出现，不得提前剧透。
- 录屏/操作演示结束后，必须切回原始全屏人物口播画面，恢复同一主口播源时间；不能让演示背景、圆窗或操作标注继续残留。
- 第二段 MG/提示词操作录屏默认对提示词正文/关键描述区域做一次平滑放大追踪：先小幅进入，再缓慢聚焦，最后回到稳定比例；禁止持续抖动、重复 seek、遮挡正文或使用无目的漂移。焦点框和焦点标签必须避开正文。
- 普通素材不做放大追踪；只有被识别为操作演示的素材才启用放大追踪。普通截图以 contain 保持可读，素材讲解完成后按语义窗口退出。


## 2026-08-23 本轮新增修正（最高优先级）

- 在“原来用 AI 生成的视频”独占证据段，左侧只增加与当前语义同步的三组标注：`原来用ai做出来的视频效果`、`非常简陋`、`很差`。三组采用错峰入场、统一协调退场，卡片位置必须避开证据窗口，不得重叠。
- `LineIconBadge` 支持少量 `boxed` 圆角矩形卡片、`edgeGlow` 柔和边缘光和可配置圆角；不是所有组件都套卡片，默认仍使用无框编辑风格。
- `SpeakerBubble` 禁止厚重纯黑外圈；改用柔和彩色描边、很弱的白色内圈和轻微外发光。只在证据/操作演示确实需要时出现。
- 全屏人物口播已经完整显示时，必须移除左下人物缩放框；证据/操作演示结束后要切回全屏人物，并清理残留小窗和标注。
- 证据素材必须稳定播放，不能抖动、重复 seek、闪屏或卡帧；插入证据期间关闭普通口播包装，证据结束后恢复主口播源的正确时间。
- 渲染验收必须抽查证据三组卡片、证据结束后的恢复首帧、所有带 `SpeakerBubble` 的操作演示段，确认无黑边、无残留、无组件相交。

## 2026-08-23 本轮最终修正：无动效口播与右侧案例素材（最高优先级）

- “一定要打好地基”这类明确要求不做包装的语义区间，只保留原始全屏人物口播与严格同步字幕；关闭背景板、人物缩放框、MG、信息卡、标题和装饰组件。
- “抖音各大博主发的关于ai自动剪视频的录屏”属于普通案例素材，不是操作演示：从该语义段开始稳定放在人物画面右侧，不做鼠标追踪、放大追踪、持续抖动或重复 seek；素材按 `startFrom=0` 连续播放。
- 右侧案例素材出现期间，所有解释性组件统一排到左侧安全区；右侧标题、卡片、数据条和其他组件必须隐藏或提前退场，素材窗口与任何组件不得相交，也不得挡人物脸、字幕或嘴部。
- 案例素材结束后恢复普通口播包装；切换首帧、末帧和恢复首帧必须抽帧检查，确认没有黑帧、闪屏、卡帧、残留小窗或组件重叠。
- 生成脚本时必须先建立素材窗口、人物安全区、组件区和字幕区的矩形表；渲染前后逐帧检查相交，发现相交优先退场/迁移组件，不得用半透明叠放掩盖问题。



## 2026-08-25 布局与配色补充规则（本期反馈固化）

- **紧凑排版**：左右信息组采用紧凑纵向排版；标题、说明、小卡片之间不得留下大块空白。默认标题与说明间距 3–6px，说明与下一组件间距 14–28px；只有语义分组变化时才增加间距。
- **人物中心保护**：人物是画面中心主体。普通信息组件不得放在人物头顶、脸部、嘴部、手部或中心轴线上；默认从人物安全边界外布置，必要时向左右边缘迁移。组件应足够大，保证在 1920×1080 成片中清晰可读。
- **素材区域互斥**：展示截图、录屏或视频素材时，素材展示矩形为独占区域；该区域内及其贴边安全区禁止叠加普通信息卡、标题、图标、线条和装饰组件。素材展示优先保证可读性，素材结束后再恢复包装。
- **配色多样性**：字幕继续只用亮白；信息组件不可全部白色。按语义轮换使用青、蓝、紫、黄、红等强调色，白色作为主体文字，颜色只用于标签、边缘、数字、图标或局部强调，保持克制发光。
- **不重叠验收**：每个组件必须声明安全矩形，组件、人物脸部、字幕、素材窗口之间不得相交；禁止依赖 z-index 遮盖重叠。每次改版抽查入场帧、稳定帧、交接前帧和交接后帧。

## 2026-08-27 组件库增强与录屏背景规则（本期反馈固化）

- 录屏/截图素材如果已经自带完整背景，禁止再叠加背景板、主题艺术字、渐变地板或重复包装；素材展示区保持原画面完整可读，只允许轻量边界（如确有必要）。
- 右侧及左右边缘信息组件必须在 1920×1080 成片中清晰可读：中文主信息优先 28px 以上，副标签 11–16px；不得以小字号堆叠替代层级。
- 信息组件采用“白色主体文字 + 语义强调色”而不是纯白堆叠，强调色可轮换青、蓝、紫、黄、红、绿；颜色用于图标、边框、关键词、数字和状态，不覆盖整块文字。
- 已沉淀的图标组件必须真正调用：优先 `LineIconBadge`；需要更紧凑的图标+标签时使用 `IconLabelChip`。当前共享图标语义包括 `ai`、`layers`、`download`、`user`、`check`、`warning`、`flow` 等。
- 参考图风格沉淀为通用组件层级：线性图标、彩色描边/柔和发光、中文主标题、英文副标签、少量圆角卡片、流程节点与状态标签。
- 本规则与安全矩形、不重叠、人物中心保护、字幕纯白和素材独占规则同时生效。


## 2026-08-27 v3 用户反馈沉淀（QQ 空间数据恢复）

本轮视觉验收规则优先于旧版同类描述：

- **录屏独占素材原则**：当用户录屏本身已经带完整背景时，证据段只使用原录屏素材、口播缩放框和必要说明组件；禁止叠加 `DemoBackdrop`、背景板、重复渐变或第二层氛围底图。录屏切入/切出使用直接切换或极短的明确交接，不与人物底图叠化。
- **信息组件留白**：上下排列的信息卡、步骤卡和说明组件必须保留清晰纵向行间距；不得因统一定位或紧凑排版而黏连。信息卡之间默认至少保留 28px 的安全间隔，并在实际成片抽帧确认。
- **固定左上编辑栏**：需要时在全片使用 `PersistentEditorialBar`，位置、文案、颜色和结构从首帧到尾帧保持不变，不随场景切换、证据段或 CTA 改变。
- **双语字幕**：按参考片需要时使用 `BilingualSubtitle`，中文主字幕在上、英文副字幕在下，两行分别使用黑色半透明底条；字幕整体底部居中，中文字号优先 42px 左右、英文 28px 左右，并检查长句安全宽度。
- **克制发光**：`LineIconBadge`、`IconLabelChip`、`TechCard`、`DemoPanel`、`SpeakerBubble` 可按语义启用轻微 `edgeGlow`；发光只服务于图标、边框和状态层级，不得形成大面积泛光或压过人物、录屏和字幕。
- **沉淀要求**：以上规则必须同步到共享组件源码、README、组件规范、项目剪辑计划、风格决策和工作流状态；目标工程必须通过 TypeScript 与 Remotion composition 检查后才允许渲染。
- **无黑底证据段**：当用户明确要求“不要有黑底、直接用原录屏素材”时，必须让原录屏以 `cover` 铺满整个输出画布（同画幅优先 1920×1080），禁止用局部 `contain` 窗口在黑色底图上留出大块空白；只叠加必要说明组件和口播缩放框。


## 2026-08-27 v5 用户反馈：左上短栏与口播缩放框同步

- `PersistentEditorialBar` 只放在屏幕左上角；本期固定栏采用短宽度（建议 420–560px），可省略右侧文案，横线不得延伸到人物主体或穿过人物头部。
- `SpeakerBubble` 若位于延迟开始的 `Sequence` 中，源时间必须对齐该 Sequence 的全局起点（例如 `sourceStartFrame={startFrame}`），不可继续从源视频第 0 帧开始；缩放框必须静音，避免重复音轨。
- 成片验收必须同时检查证据段首帧、中段和尾帧：缩放框嘴型、主口播音频、字幕和当前口播内容保持同一全局时间轴。
## 2026-08-31 反馈固化：录屏语义标签、侧栏布局、数字范围与人脸校准

- **录屏标签按内容命名**：禁止使用“真实平台录屏 / SCREEN RECORDING”等强调素材来源的标签；改为说明当前内容的普通语义标签，例如“任务内容讲解 / TASK GUIDE”。
- **说明组件默认左侧**：人物主画面和录屏段的信息组件优先使用左侧窄栏，右侧仅用于偶尔的节奏变化；不得持续压住人物脸部、嘴部或素材关键信息。
- **数字范围增长**：时薪、价格、数量等范围优先使用 `AnimatedRangeMetric`。从起始值增长到目标值，目标值只在语义到达后稳定显示；增长过程配低音量连续提示音，稳定帧配一次完成/收银提示音。
- **人物小窗逐段校准**：`SpeakerBubble` 除保持 `sourceStartFrame={startFrame}` 与静音外，还必须在该素材段的前、中、后多个时间点抽帧校准 `objectPosition`，不能沿用未经验证的全局默认值。
- **长录屏增加解释密度**：按 SRT 的语义节点增加说明组件；同一时刻通常只保留一个主说明，最多一个辅助标签。组件表达含义，不逐字重复字幕。
- **录屏仍遵守无额外背景**：源录屏已有完整背景时继续全画布 `cover`，不添加黑底、背景板、重复渐变或第二层氛围底图。

## 2026-09-01 v4 硬性规则

### 人物缩放框固定左下角
- 只要场景中显示人物缩放框，必须使用画面左下安全区域。
- 不得因为说明组件、节奏或左右布局将人物缩放框移动到右侧。
- 推荐统一使用 `left:54, top:690, width:270, height:270` 或等价左下安全矩形。
- 人物缩放框不得遮挡字幕、人物关键口型、录屏关键任务区域。
- 渲染验收必须抽查所有显示人物缩放框的场景，确认首帧、中段、尾帧均位于左下角。

### 字幕结尾标点
- 每条字幕 cue 末尾不得出现句号、逗号、问号、感叹号、顿号、分号、冒号、省略号等标点。
- 句子内部标点可以保留。
- 导入 SRT 和渲染字幕组件都要自动清理 cue 末尾标点，防止数据源或手写文案回流。
- 验收时逐条检查实际渲染字幕文本。

### 录屏与口播精准同步
- 当口播说出具体任务名称时，录屏底层必须同步展示对应任务卡、详情页或点击区域。
- “方言数据整理” cue 开始时必须已经定位到方言任务。
- “英语陪练师” cue 开始时必须已经定位到英语陪练任务。
- “特斯拉车主” cue 开始时必须已经定位到特斯拉车主访谈任务。
- 后续方言、音乐测评、专业任务等必须与实际页面内容匹配。
- 不得只使用浮层标题替代真实画面定位。
- 必须通过录屏时间映射、分段剪辑、seek 或合适的素材切点完成同步。
- 录屏自带完整背景时，继续使用原录屏全画布 `cover`，不得添加黑底、背景板、重复渐变或第二层氛围底图。



## 2026-09-01 Microduck v2 执行硬规则

- 用户提供的机器鸭视频从口播“Microduck身高25厘米”（约 20.6 秒）开始作为全屏讲解素材使用；同画幅优先 1920×1080，保持原画面比例和内容完整，不人为裁剪，不叠加新的黑底/背景板。
- “从模拟器部署到真机的整个流程都开源了”（约 69.733–73.1 秒）必须切换到 `GitHub上开源的机器鸭.mp4`，不得用标题或占位卡替代。
- 人物全屏口播期间禁止自动生成左下人物缩放框；只有全屏机器鸭讲解素材期间才显示人物缩放框。缩放框始终固定左下、静音，并使用与当前全局口播帧一致的 `sourceStartFrame`。
- 所有金额、价格和数量视觉节点必须使用从小到大的递增动画；金额静态标题应拆为独立数字组件，避免提前显示目标值。
- 组件不得大面积纯黑；同一画面避免堆叠多个矩形黑框，交替使用无框图标标签、彩色描边、短线、数字组件和轻微发光。
- 删除跨人物/跨屏的黄色装饰横线；固定左上短栏的线条必须保持短，不越过人物主体。
- 字幕 cue 末尾继续自动清理中英文标点；不生成插画，除非用户另行明确要求。

## 2026-09-02 本轮沉淀：组件减框、动效与 MP4 素材

- 参考已确认的前期成片组件体系：避免整屏由矩形卡片组成；优先使用无框大标题、彩色短线、圆形图标、数字指标、流程节点和少量胶囊标签交替编排。矩形卡片只在确有信息承载需要时局部使用。
- 所有信息组件必须有明确的进入和退出动画（淡入、位移、缩放、逐项揭示等），并在语义切点配合低音量对应音效；音效不得盖过主口播。
- 用户提供的 MP4 属于真实证据素材时，应根据 SRT 语义放在最合适的口播位置，使用稳定播放和正确的 `contain`/`cover`，不得额外叠加背景板、黑底或重复渐变；播放期间与普通信息组件互斥。
- 成片前抽查 MP4 首帧、播放中、末帧和恢复口播帧，确认无黑帧、变形、卡帧、重复 seek 或残留组件。

## 2026-09-03 动画案例素材与透明案例展示硬规则

- 用户以 CSV 提供“动画片段名称 + 对应原 SRT 口播时间轴”时，CSV 是动画入点的权威映射。每个动画从对应语义开始的同一帧入场；不得以章节标题、猜测时间或静态首帧替代。
- 每个用户 MP4 动画必须以可播放视频层连续播放，且抽查首帧、播放中段、尾帧和离场后一帧。禁止冻结首帧、重复 seek、黑帧、截帧冒充动画或循环补足不足时长。
- 动画素材默认 `muted`；主口播音频由唯一的主口播 `Audio` 轨连续提供，动画音轨不得覆盖或干扰人声。
- CSV 对应动画覆盖层的底部继续保留原口播画面，推荐使用约 0.65–0.76 的整体透明度和 8–12 帧淡入淡出；除非用户明确要求全屏替换，不得铺设整屏黑底、黑幕或新的背景板。
- 动画放置在对应语义位置时，已有且正确的组件、字幕和人物安全布局不得改写。动画层应位于底层口播之上、组件之下；只新增播放层，不重排原组件。
- “案例展示/案例演示”需要衬底时，优先复用原口播作为可见底层；禁止整屏纯黑。若需要降噪，只允许低强度、无色或近中性遮罩，确保口播主体仍清楚可见。
- 普通信息组件禁止使用半透明彩色长方形背景。优先使用无框标题、短强调线、圆形图标、胶囊标签、数字、节点和轻微发光。字幕可保留中性深色可读性底板；真实图片/视频素材可保留必要的细边框。
- 交付前必须从最终导出的 MP4 抽查动画段，并确认主口播音频存在、动画音轨静音、动画内容连续变化、案例段非全黑。
## 2026-09-10 组件库优先与独立动效库禁用规则（最高优先级）

- 独立动效库已从本 Skill 删除；禁止恢复、生成、复制或调用 `ShotcraftEffect`、`ShotcraftTransition` 及其派生效果。
- 剪辑只使用本 Skill 的信息组件、真实素材、基础场景转场与共享音效库。组件自身允许克制的入场/退出，但不得演变为独立炫技动效。
- 转场仅使用 `SceneTransition` 或硬切、短淡化、轻推、简单遮罩；不得用黑幕、强闪烁、三维飞行墙或大面积扫描框。
- 用户提供的 MP4 动画只能作为普通媒体素材，并仅在用户明确要求或时间轴表明确映射时使用；不得把它登记为动效库组件。
- 生成 `edit-script.json` 时，组件名必须来自 `components.tsx`；不得出现任何 Shotcraft 名称或 `effect` 路由字段。

